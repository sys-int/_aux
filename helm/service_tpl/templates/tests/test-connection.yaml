apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SERVICE_TPL.fullname" . }}-test-connection"
  labels:
    {{- include "SERVICE_TPL.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "SERVICE_TPL.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "SERVICE_TPL.fullname" . }}
  labels:
    {{- include "SERVICE_TPL.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "SERVICE_TPL.selectorLabels" . | nindent 4 }}
